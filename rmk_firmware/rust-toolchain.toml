# Pinned: the locked RMK revision does not build with rustc 1.98 (pin! of a
# slice no longer coerces). 1.94.1 is the toolchain verified on hardware.
[toolchain]
channel = "1.94.1"
components = ["llvm-tools"]
targets = ["thumbv7em-none-eabihf"]
